import ReactMarkdown from "react-markdown";

export default function Blog2() {
    return (
        <ReactMarkdown className="text-4xl">
            ## Hello, *World*
        </ReactMarkdown>
    );
}